"""Research Agent v3 core layer.

Sub-packages:
    contracts   — module and data contracts
    exceptions  — unified exception hierarchy
    state       — state machine and checkpoint management
    validation  — input/output validators and quality assessor
    provenance  — provenance tracking and integrity verification
"""
